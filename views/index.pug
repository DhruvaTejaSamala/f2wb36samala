extends layout

block content
  h1= title
  p Welcome to the Assignment  #{title}
  p Yesterday all my troubles seemed so far away.
  p Now it looks as though they're here to stay
  button(type="button", class="btn btn-secondary", onclick = "doBump()") Press Me
  #bump 
    p Bump the count. 
  script(type='text/javascript').
    let count = 0
    let bump = 1
    function doBump(){
      count+=bump 
      bump++
      document.getElementById("bump").innerHTML='Count '+count + "with bump "+ bump
    }
  a(href='peterGriffen') PeterGriffen
